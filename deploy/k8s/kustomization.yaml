apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply with:  kubectl apply -k deploy/k8s
# Remove with: kubectl delete -k deploy/k8s
resources:
  - namespace.yaml
  - web.yaml
  - ingress.yaml

images:
  - name: insureagentlabs-web
    newTag: latest
